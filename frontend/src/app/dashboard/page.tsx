import PageShell from "@/components/layout/PageShell";

export default function DashboardPage() {
  return (
    <PageShell noHeader>
      <p className="text-muted-foreground text-sm mt-4">Dashboard — rebuilding</p>
    </PageShell>
  );
}
