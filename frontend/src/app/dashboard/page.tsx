import PageShell from "@/components/layout/PageShell";

export default function Page() {
  return (
    <PageShell title="dashboard">
      <p className="text-muted-foreground text-sm mt-4">Coming soon — rebuilding dashboard page</p>
    </PageShell>
  );
}
