import PageShell from "@/components/layout/PageShell";

export default function TrackerPage() {
  return (
    <PageShell title="Tracker">
      <p className="text-muted-foreground text-sm mt-4">Tracker — rebuilding</p>
    </PageShell>
  );
}
